# Documentation

- `policies/` contains the contributor and engineering policies referenced by
  the root `AGENTS.md`.
- `adr/` records architecture decisions.
- `ENFORCEMENT-CHECKLIST.md` and `ENFORCEMENT-MANIFEST.md` describe the checks
  that keep those policies executable.
- `test-evidence.json` records the current test evidence contract.
